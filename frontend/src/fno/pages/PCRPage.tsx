import { TerminalPanel } from "../../components/terminal/TerminalPanel";

export function PCRPage() {
  return (
    <TerminalPanel title="PCR" subtitle="Stage 6 UI placeholder">
      <div className="text-xs text-terminal-muted">PCR dashboard UI will be added in Stage 6. Backend endpoints are live.</div>
    </TerminalPanel>
  );
}
