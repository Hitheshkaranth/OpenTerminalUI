import { TerminalPanel } from "../../components/terminal/TerminalPanel";

export function HeatmapPage() {
  return (
    <TerminalPanel title="Heatmap" subtitle="Stage 6 UI placeholder">
      <div className="text-xs text-terminal-muted">Heatmap UI will be added in Stage 6. Backend endpoints are live.</div>
    </TerminalPanel>
  );
}
