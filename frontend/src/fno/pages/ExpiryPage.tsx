import { TerminalPanel } from "../../components/terminal/TerminalPanel";

export function ExpiryPage() {
  return (
    <TerminalPanel title="Expiry Dashboard" subtitle="Stage 6 UI placeholder">
      <div className="text-xs text-terminal-muted">Expiry day dashboard UI will be added in Stage 6. Backend endpoints are live.</div>
    </TerminalPanel>
  );
}
