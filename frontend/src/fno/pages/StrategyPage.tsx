import { TerminalPanel } from "../../components/terminal/TerminalPanel";

export function StrategyPage() {
  return (
    <TerminalPanel title="Strategy" subtitle="Stage 6 UI placeholder">
      <div className="text-xs text-terminal-muted">Strategy Builder UI will be added in Stage 6. Backend endpoints are live.</div>
    </TerminalPanel>
  );
}
